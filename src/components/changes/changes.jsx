import React from "react";
import BreakChange from "./breakChanges";
import SessionChanges from "./sessionChanges";
function Changes() {
  return (
    <div id="changes">
      <BreakChange />
      <SessionChanges />
    </div>
  );
}

export default Changes;
